You are a senior React + TailwindCSS + Framer Motion engineer.
Refactor ONLY the “How GarageWala Works” section for excellent MOBILE UX (≤768px). Desktop layout should remain unchanged.

## Current issues
- Icons and spacing are oversized on mobile → excessive scrolling.
- Steps stack with big gaps; text wraps to long paragraphs.

## Goals (mobile-first)
- Compact, scannable steps.
- Clear visual rhythm; less vertical height.
- Subtle animations; fast LCP.

## Implement these exact changes

### 1) Section wrapper
- Mobile defaults: `px-4 pt-8 pb-10`
- Heading: `text-2xl font-semibold text-center leading-tight`
- Subheading: `text-sm text-white/70 mt-2 text-center max-w-[28ch] mx-auto`

### 2) Steps layout (mobile)
- Replace 4 big columns with **card list** or **2×2 grid** that collapses to 1 column under 360px.
- Container:
  - `grid grid-cols-2 gap-3 mt-6 md:grid-cols-4`
  - On very small screens: `@media (max-width: 360px){ grid-template-columns: 1fr; }`
- Each step card (mobile styles first):
  - `rounded-2xl p-3 bg-white/5 border border-white/10 backdrop-blur`
  - `flex flex-col items-center text-center`
  - Icon chip: `size-12 rounded-2xl shadow-sm` (use existing gradient)
  - Step label (STEP 01): `text-[11px] uppercase tracking-wide text-white/60 mt-2`
  - Title: `text-base font-semibold mt-1`
  - Caption: `text-[12px] text-white/70 mt-1 leading-snug line-clamp-2` (truncate to 2 lines)

### 3) Optional horizontal scroll (if you prefer 1-per-view)
- Alternate mobile layout:
  - Wrapper: `mt-6 -mx-4 px-4 overflow-x-auto no-scrollbar`
  - Inner row: `flex gap-3 w-max`
  - Card width: `w-[78vw] max-w-[320px]`
  - Keep desktop grid via `md:grid md:grid-cols-4 md:gap-6 md:overflow-visible md:w-auto`

### 4) Animation (Framer Motion)
- Stagger fade-up for cards on enter:
  - container: `variants={{show:{transition:{staggerChildren:0.06}}}}`
  - item: `initial={{opacity:0,y:14}} animate={{opacity:1,y:0}} transition={{duration:0.35}}`
- Icon chip micro-bounce on hover: `whileHover={{scale:1.05}}`

### 5) Spacing rules (mobile)
- Reduce vertical gaps between rows to 12–16px.
- Keep total section height compact (avoid > 90vh).
- Ensure all text fits without overflow; clamp long captions.

### 6) Accessibility
- `<section aria-labelledby="how-heading">`
- Icons with `aria-hidden="true"`, titles as `<h3>` inside each card.
- Touch targets ≥44px; semantic lists (`<ul><li>`).

### 7) Keep desktop intact
- Use `md:` prefixes to restore your current desktop styles:
  - `md:text-4xl md:leading-tight` for heading
  - `md:grid-cols-4 md:gap-6` for steps
  - Larger icons/title sizes behind `md:` only.

## Deliverables
- Updated JSX/TSX for the “How it works” section using Tailwind util classes above.
- Framer Motion variants for container + items.
- No visual regressions on desktop.

## Acceptance checks (375×812)
- Heading + subheading visible without huge gaps.
- Steps appear in a compact grid (2×2) or smooth horizontal scroll list.
- Each card shows: icon, STEP label, title, 2-line caption.
- No overflow or excessive scrolling; section feels tight and polished.
