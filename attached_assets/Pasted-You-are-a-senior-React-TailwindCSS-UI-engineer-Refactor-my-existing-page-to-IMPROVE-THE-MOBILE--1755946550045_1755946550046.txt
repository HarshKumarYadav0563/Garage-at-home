You are a senior React + TailwindCSS UI engineer. Refactor my existing page to IMPROVE THE MOBILE (≤768px) EXPERIENCE ONLY. Keep desktop styles unchanged.

## Goals (Mobile-first)
- Reduce vertical height and scrolling in the hero.
- Make CTAs obvious and inline.
- Compress trust badges and metrics.
- Keep imagery lightweight and secondary to text.
- Faster LCP (avoid heavy images above the fold).

## Exact changes to implement (Tailwind + structure)

### 1) Header / Top bars
- Collapse the top utility bar (cities, rating, guarantee, phone) into a single horizontally scrollable row with small pills:
  - Use `overflow-x-auto no-scrollbar flex gap-2 px-3 py-1`
  - Each pill: `text-xs px-2 py-1 rounded-full bg-white/70 border border-black/5 backdrop-blur`
- Logo row:
  - Left: logo + “Premium Doorstep Service” on one line (truncate if needed).
  - Right: menu icon.
  - Add city selector as a small pill below the logo row (not inline) with: `mt-2 text-xs px-2 py-1 rounded-full bg-white shadow-sm`

### 2) Hero (make compact)
- Limit hero height to ~48–52vh on mobile:
  - Section classes: `pt-6 pb-4 min-h-[48vh]`
- Heading:
  - `text-2xl sm:text-3xl leading-tight font-semibold`
  - Max 2 lines: `line-clamp-2`
- Subheading:
  - `text-sm text-black/70 mt-2 line-clamp-2`
- CTA row (inline):
  - Wrap in `grid grid-cols-2 gap-2 mt-4`
  - Primary button: `h-11 rounded-full text-sm font-semibold bg-gradient-to-r from-emerald-500 to-indigo-500 text-white shadow-md`
  - Secondary button: `h-11 rounded-full text-sm border border-black/10 bg-white text-black flex items-center justify-center gap-1`
- Trust row (single line):
  - `mt-3 flex flex-wrap items-center gap-x-3 gap-y-2 text-xs text-black/70`
  - Items: ⭐ 4.9/5 • 👥 25k+ • ⏱ 2hr
- Hero image:
  - Move **below** trust row and make SMALL:
  - Container: `mt-4 rounded-2xl overflow-hidden border border-black/5 shadow-sm`
  - Image: `w-full h-36 object-cover`
  - Defer loading with `loading="lazy"`; use a lighter image source on mobile.

### 3) Floating action button (optional)
- Add a sticky bottom action on mobile only:
  - Wrapper: `fixed inset-x-3 bottom-3 z-40`
  - Button: `h-12 rounded-full bg-black text-white shadow-lg text-sm font-semibold`
  - Text: “Book in 60s — Use My Location”

### 4) Stats cards (below hero)
- Replace 2×2 large cards with a tight two-column grid:
  - `grid grid-cols-2 gap-3 px-3 mt-4`
  - Card: `rounded-xl p-3 bg-white border border-black/5`
  - Title: value in `text-lg font-bold`
  - Caption: `text-[11px] text-black/60`
- Keep only the top 3 metrics on mobile: **Mechanics**, **Services**, **Rating**. Hide the 4th (Cities) with `hidden md:block`.

### 5) Spacing + typography scale (mobile)
- Reduce global paddings: sections use `px-3`–`px-4`, headings `text-2xl`, sub `text-sm`.
- Consistent rhythm: space in 6/8/12 px steps (avoid 20–28px gaps).

### 6) Animations (subtle)
- Use Framer Motion fade-up on hero elements with small delays (0.05–0.1s).
- Disable large floating icon animations on mobile (`md:` prefix them).
- Respect `prefers-reduced-motion`.

### 7) Accessibility & performance
- Buttons ≥44px height; labels/ARIA on location/phone.
- Use `decoding="async"` and `loading="lazy"` on hero image.
- Avoid layout shift: set explicit width/height or aspect ratio on images.

## Deliverables
- Update the existing React components; DO NOT rewrite desktop.
- Provide a single PR diff or updated components with `md:` prefixes for desktop styles and mobile-first defaults as above.

## Acceptance checks (375×667)
- Hero fits in ~50vh with visible heading, subheading, inline CTAs, trust row, and a small image.
- No text overlaps; no image above text; CTAs are thumb-friendly.
- Stats render in a compact two-column grid with three items.
